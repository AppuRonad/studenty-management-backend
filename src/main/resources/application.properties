spring.application.name=student-management
server.port=8080

# MongoDB Atlas (cloud) — falls back to localhost for local dev
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://studmgm:SmsPro2026@studmgm.zrimkpw.mongodb.net/studentdb?retryWrites=true&w=majority&appName=studmgm}
spring.data.mongodb.database=studentdb

# CORS
spring.web.cors.allowed-origins=*

server.forward-headers-strategy=framework
